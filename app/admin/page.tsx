import {getShops} from '@/lib/data'; import {ImportForm} from './ui';
export const dynamic = "force-dynamic";
export const metadata={title:'管理/OCR取り込み | 愛知トレカ買取ナビ',description:'買取表テキストやOCR結果を取り込み、店舗別買取価格として登録する管理画面です。'};
export default async function AdminPage(){const shops=await getShops(); return <div className="grid grid2"><div className="card"><h2>買取表テキスト取り込み</h2><p>本番はX API → 画像OCR → このフォームに渡す流れです。</p><ImportForm shops={shops}/></div><div className="card"><h2>本番で追加する処理</h2><div className="list"><div className="row"><div><strong>X API取得</strong><small>登録済み公式アカウントの投稿だけ取得</small></div><span className="tag tagBlue">次段階</span></div><div className="row"><div><strong>画像OCR</strong><small>Google Vision / AWS Textract等に差し替え</small></div><span className="tag tagOrange">重要</span></div><div className="row"><div><strong>承認画面</strong><small>信頼度が低いカード照合は人間確認</small></div><span className="tag tagGreen">必須</span></div><div className="row"><div><strong>価格の期限切れ</strong><small>本日限り/7日間などを自動判定</small></div><span className="tag tagRed">必須</span></div></div></div></div>}
